{
  "name": "kagi-search-cli",
  "version": "1.0.0",
  "description": "基于 Playwright 的 Kagi 搜索 CLI 工具",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist//src/index.d.ts",
  "bin": {
    "kagi-search": "./bin/kagi-search",
    "kagi-search-mcp": "./bin/kagi-search-mcp"
  },
  "scripts": {
    "build": "tsc",
    "postinstall": "playwright install chromium",
    "dev": "ts-node src/index.ts",
    "start": "node dist/src/index.js",
    "test": "ts-node src/index.ts \"playwright typescript\"",
    "test:build": "npm run build && node dist/src/index.js \"playwright typescript\"",
    "debug": "ts-node src/index.ts --no-headless \"playwright typescript\"",
    "debug:build": "npm run build && node dist/src/index.js --no-headless \"playwright typescript\"",
    "link": "npm link",
    "clean": "node -e \"const fs = require('fs'); const path = require('path'); if (fs.existsSync('dist')) fs.rmSync('dist', { recursive: true, force: true });\"",
    "clean:all": "bash scripts/clean.sh",
    "clean:full": "bash scripts/clean.sh --full",
    "mcp": "ts-node src/mcp-server.ts",
    "mcp:build": "npm run build && node dist/src/mcp-server.js",
    "test:env": "ts-node check-env.ts",
    "test:mcp": "ts-node test-mcp.ts",
    "test:mcp:full": "ts-node test-mcp.ts full",
    "test:all": "npm run test:env && npm run test:mcp",
    "validate:mcp": "ts-node validate-mcp.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/web-agent-master/kagi-search.git"
  },
  "homepage": "https://github.com/web-agent-master/kagi-search#readme",
  "bugs": {
    "url": "https://github.com/web-agent-master/kagi-search/issues"
  },
  "keywords": [
    "kagi",
    "search",
    "cli",
    "playwright",
    "automation",
    "mcp",
    "claude"
  ],
  "author": "web-agent-master",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.13.9",
    "jsdom": "^26.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "commander": "^13.1.0",
    "dotenv": "^17.2.1",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0",
    "playwright": "^1.50.1",
    "zod": "^3.22.4"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
